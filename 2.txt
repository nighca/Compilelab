PROGRAM test1;

CONST cccc = 0;
INTEGER:a, b, c;
REAL:d;
BOOLEAN:ok;

BEGIN
    a := 5;
    c := 0;
    d := 3.5;
    ok := TRUE;
    IF ok THEN a := (b + c) / d + a;

    FOR i := 1 TO 10 DO
        d := d * c;

END
